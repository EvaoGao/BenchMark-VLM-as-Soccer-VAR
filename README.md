# Capstone Project for DS 440

Complete consistent data is the finalize to-use data. 'Data manipulation' are the codes for data transformation. 'experiment.ipynb' is the codes to run the experiment on Gemini.'frame_extraction.py' contains the functions to do frame extraction. 'gemini_call' is the codes to input URLs of video and get gemini response. Other codes are for traditional models.


